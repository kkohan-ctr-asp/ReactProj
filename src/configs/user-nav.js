import routes from './routes';

const NAV_ITEMS = [
  { name: 'Account', path: routes.account },
  { name: 'Order history', path: routes.orderHistory },
  { name: 'Meal planner', path: routes.planner },
];

export default NAV_ITEMS;
